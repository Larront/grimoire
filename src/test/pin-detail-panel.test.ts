// Tests migrated to pin-details.test.ts — PinDetailPanel superseded by PinDetails + DetailPanel shell.
// This file intentionally left minimal to avoid duplicating coverage.
import { describe, it, expect } from "vitest";

describe("PinDetailPanel (superseded)", () => {
  it("has been replaced by PinDetails — see pin-details.test.ts", () => {
    expect(true).toBe(true);
  });
});
